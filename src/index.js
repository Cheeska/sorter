class Sorter {

  constructor() {
    let mass =  new Array();
  }

  add(element) {

    mass.add(element);

  }

  at(index) {
    // your implementation
  }

  get length() {
    console.log(mass.length);
  }

  toArray() {
    // your implementation
  }

  sort(indices) {
    // your implementation
  }

  setComparator(compareFunction) {
    // your implementation
  }
}

module.exports = Sorter;